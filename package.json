{
  "name": "the-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"OK!\"",
    "format": "pretty-quick --staged --pattern \"**/*.{ts,tsx}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3"
  }
}
