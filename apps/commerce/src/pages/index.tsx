import { Button } from 'ui-react';

const Home = () => (
  <div>
    <h1>Hi from Commerce</h1>
    <Button color="violet" css={{ padding: 400 }}>
      Click Me
    </Button>
  </div>
);

export default Home;
